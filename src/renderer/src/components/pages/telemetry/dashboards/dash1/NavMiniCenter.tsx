import { NavMini } from '../../widgets'

/** Mini turn-by-turn nav in the cluster centre. NavMini self-positions on the divider line. */
export function NavMiniCenter() {
  return <NavMini iconSize={84} />
}
